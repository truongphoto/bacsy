DOCTOR RUSH v1.0.18 — CONTINUOUS ITEMS VALIDATION

PASS — JavaScript syntax checked with Node.
PASS — manifest.webmanifest and version.json are valid JSON.
PASS — Version/build synchronized at v1.0.18 / build 18.
PASS — Common item groups use random quantity 2–6.
PASS — Initial run immediately creates an in-frame item cluster.
PASS — Continuous refill triggers when visible item count drops below 2.
PASS — Common item rows spawn inside the visible frame instead of outside the right edge.
PASS — Monte Carlo geometry: 60,000 generated rows across PC/mobile widths remained visible and within reachable vertical limits.
PASS — Item corridor uses directional obstacle safety margins and delays the next obstacle encounter.
PASS — Normal extra item rows are capped to avoid excessive clutter.

Design target: the playfield should almost always show collectible activity; if an active hazard temporarily occupies every safe corridor, safety takes priority and refill retries rapidly rather than baiting the player into a collision.
